# Copy to ".env" only if you need to override the API location.
# Leave blank for local dev (requests go through the Vite proxy to localhost:3000).
# In production (Vercel) set this to the deployed backend URL, e.g.
#   VITE_API_BASE_URL=https://landed-api.onrender.com
VITE_API_BASE_URL=
